import { createContext } from "react";

/**
 * DataContext Definition
 * * Creates the React Context object for holding the application's data (contacts).
 * Initialized with null.
 */
export const DataContext = createContext(null);
